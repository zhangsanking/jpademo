package com.example.jpademo.repositry2;

import com.example.jpademo.domain.User;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

//@Repository
//public interface UserD extends JpaRepository<User,Long> {
   // User findByName(String name);
   // @Query(value="select * from user where id=(select max(id) from user)",nativeQuery=true)
    //User findMaxIdUser();


//}
